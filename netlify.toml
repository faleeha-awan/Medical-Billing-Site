# Netlify Configuration for BillSureMD

[build]
  # No build command needed for static site
  publish = "."

[functions]
  directory = "netlify/functions"

# Redirects for clean URLs
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200